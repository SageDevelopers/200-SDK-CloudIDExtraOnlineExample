
@model IEnumerable<RemoteWebsite.Models.Department>
@{
    ViewBag.Title = "Index";
}

<h2>Departments</h2>
<ul>

    @foreach (RemoteWebsite.Models.Department item in Model)
    {
    <li>@item.id, @item.code, @item.name</li>
    }

</ul>

